# Docker files

Setup instructions are in the [main README](../README.md).

- `config.php`: app config, read entirely from environment variables
- `apache-security.conf`: blocks direct web access to code, config and dotfiles
- `create-db-user.sql.example`: template for the least-privilege MySQL user
